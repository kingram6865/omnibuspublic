// import { getBook } from "../../services/createToc";

export const Test = async (props) => {
  // const TOC = await getBook('equity', 17);
  // // console.log(TOC)
  // const TestJSX = TOC.map(item => {

  // })

  return (
    <div className="test-info">
  
    </div>
  )
}